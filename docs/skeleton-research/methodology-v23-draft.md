# METHODOLOGY v2.3 — Draft Insertions for Review

7 reconciliation gaps between v2.0 (intent growth + retrospective revision +
categorical principles) and v2.1/v2.2 (place generativity + disclosure preview
+ register neutrality). No new sections required — all resolutions are 1-2
sentence inserts into existing sections.

Review each gap. Once confirmed, commit directly as:
`docs: METHODOLOGY v2.3 — close 7 v2.0/v2.2 reconciliation gaps`

---

## Gap 1 — Place-carrier evolution uses §10's mechanism

**Target section:** §17 Place Generativity → "Where the test lives" subsection,
end of the final paragraph (after the ✧ sentence ending "that is the editor's
verdict (§13).")

**Defers to / extends:** §10 Intent Growth (the five relations), §11
Retrospective Revision (auto-trigger)

**§13 check:** No new violation. This is a cross-reference clarification. No
new AI behavior is introduced; it reuses §10's existing machinery.

**INSERT AFTER:**
> it quotes the actual place-rendering it read and says whether, at this
> point in the draft, that rendering carries K or sits as backdrop. It
> reports what is on the page. It does not grade place-generativity —
> that is the editor's verdict (§13).

**INSERTION:**
> Place-carrier evolution follows the §10 mechanism rather than
> introducing a new axis. When the writer's relationship to the carrier
> shifts — the place builds further into K, or an earlier rendering no
> longer holds — this registers as a new intent layer with relation 揭示
> or 修正; §11 retrospective revision then applies to scenes written
> under the prior carrier assumption.

---

## Gap 2 — §11 Retrospective Revision inventory covers all four context-gated categories

**Target section:** §11 Retrospective Revision → "Intensity 1: Inventory",
after the bullet list (● ◑ ◐ ⚫ ✗) and before "The inventory is displayed
as a list."

**Defers to / extends:** §6 (all four context-gated categories: ◎ ✦ ✨ ✧)

**§13 check:** No new violation. Clarifies the scope of an existing operation;
no new AI judgment introduced.

**INSERT AFTER:**
> - **✗ Conflict**: Structurally contradicts new layer

**INSERTION:**
> The inventory covers all four context-gated categories (◎ ✦ ✨ ✧),
> not only ◎. A new intent layer may shift the structural function a
> scene must serve (✦), alter the carrier relationship of a place (✧),
> or reveal cross-scene inconsistency (✨) that was not visible against
> the prior layer. Auditing ◎ alone silently misses place-carrier drift.

---

## Gap 3 — §10 observed-trigger has a place-carrier analog

**Target section:** §10 Intent Growth → "Observed trigger" paragraph (the one
beginning "This observed-trigger flow is a carve-out from §3..."), end of that
paragraph.

**Defers to / extends:** §10 observed-trigger (carve-out logic + three-option
constraint), §17 (place-carrier)

**§13 check:** No new violation. Explicitly preserves the single-surface,
three-option constraint and "AI surfaces evidence; user decides" framing. Does
not introduce a new verdict path.

**INSERT AFTER:**
> This observed-trigger flow is a carve-out from §3 "Declaration vs
> behavior": it operates only when the cumulative evidence is strong
> (multiple scenes drifting in the same unaccounted direction), surfaces
> once per detected pattern, and always offers "incidental detail" as a
> first-class interpretation. AI surfaces evidence; user interprets.

**INSERTION:**
> The observed-trigger has a place-carrier analog. When multiple scenes'
> ✧ reports accumulate rendering on the same coordinate without a
> declared carrier role, AI may surface this once — as a question, never
> a verdict — offering three interpretations: incidental rendering detail,
> an emerging place-carrier, or something AI missed. The same
> single-surface, three-option constraint applies.

---

## Gap 4 — §18 Disclosure Preview is drive-type-independent

**Target section:** §18 The Situate Disclosure Preview → after the final
paragraph (the one beginning "The preview never blocks...")

**Defers to / extends:** §4 (Drive Types), §18 (the four preview fields)

**§13 check:** No new violation. The preview explicitly does not block; the
drive-type-independent surfacing is neutral — all four drive types are treated
identically for this step.

**INSERT AFTER:**
> The preview never blocks. A writer may proceed with a
> place-independent central question, a real-person story, or an
> undecided truth-status. The tool's only job is that the writer sees
> the gate now, while there is still time to write toward it — or to
> decide, with full information, that this work belongs somewhere other
> than Situate.

**INSERTION:**
> For drive types that do not pass through situate.map — commercial-
> purposeful, entangled, and unknown — the same four disclosure fields
> are surfaced at project creation, immediately after drive-type
> selection, as a standalone step before any module begins. The preview
> is module-independent; it is the writer's first contact with the
> downstream gates regardless of which path follows.

---

## Gap 5 — ✧ fires without a declared place-shape; baseline is the prose

**Target section:** §6 situate.at → the ✧ Serves place bullet, appended after
the existing description (after "It reports; it never grades
place-generativity (§13).")

**Defers to / extends:** §3 "Declaration vs behavior" (behavior not inferred
from writing), §9 Place Neutrality (undeclared ≠ penalized)

**§13 check:** No new violation. "Noted, not penalized" forecloses both a
penalty and an inference from undeclared state. Purely a reporting function.

**INSERT AFTER:**
> It reports; it never grades place-generativity (§13).

**INSERTION:**
> ✧ fires regardless of declared place-shape. When place-shape is
> undeclared — for entangled and unknown drives, or when the writer
> chose "I don't know" — the baseline is the prose itself: ✧ reports
> whether this scene's place-rendering carries K at this point, without
> referencing a structural plan. The undeclared state is noted as
> context; it is not penalized and not inferred from.

---

## Gap 6 — Center type and drive type are orthogonal; all 16 matrix cells valid

**Target section:** §7 The Eight Universal Angles → the paragraph beginning
"The center of gravity is not always a person." — end of that paragraph,
after "A work whose true subject is a system still anchors to the coordinate
where that system is most itself (§17, §18)."

**Defers to / extends:** §7 (re-voicing table — center type × question
re-voicing), §4 (drive type governs initialization flow only)

**§13 check:** No new violation. States that all cells are valid; makes no
ranking between drive types or center types. Reaffirms existing §7 re-voicing
mechanism as the sole adapter needed.

**INSERT AFTER:**
> A work whose true subject is a system still anchors to the coordinate
> where that system is most itself (§17, §18).

**INSERTION:**
> Drive type (§4) and center type are orthogonal. Any drive type may
> arrive at any center — person, place, institution, or system — and the
> Socratic + 归纳 algorithm handles all combinations via the re-voicing
> table above. Drive type governs the initialization flow only, not
> which center types are available to the writer.

---

## Gap 7 — Late-invoked situate.act bundles place-shape + disclosure retroactive checks

**Target section:** §5 situate.act → "The place-shape" subsection, end of the
final paragraph (after "situate.act records the coordinate(s); whether the
place carries the work is built later, in situate.at, and governed by §17.")

**Defers to / extends:** §10 observed-trigger (three-option interpretation —
each bundled component preserves it), §18 (disclosure preview), §A.9
(retroactive check mechanism) and §A.10 (Commitment Confirmation primitive) in
missing-modules-v1.md

**§13 check:** No new violation. Bundling does not compress or elide the
three-option interpretation of any component. Each structural observation
within the bundle still offers (A) accept reframe / (B) revise declaration /
(C) rewrite — the writer decides on each independently. The bundle prevents
fragmented parallel interruptions, not writer agency.

**INSERT AFTER:**
> situate.act records the coordinate(s); whether the place *carries* the
> work is built later, in situate.at, and governed by §17.

**INSERTION:**
> When situate.act is invoked late — after scenes are already written —
> the §A.9 retroactive check bundles all implicitly-made structural
> decisions into a single §A.10 Commitment Confirmation observation:
> segment structure, any ✧-pattern-implied place-shape, and an unseen
> disclosure preview (§18) are surfaced together as "you have been
> making these decisions implicitly; here is what to declare explicitly
> now." Each component preserves its three-option interpretation;
> bundling prevents simultaneous parallel retroactive flows from
> fragmenting the writer's attention.

---

## v2.3 Changelog Entry

**Heading change:** Update the document header from `v2.2` to `v2.3`.

**Target:** §19 Versioning the Methodology, insert as a new block immediately
before the "v2.2 changelog" block.

**INSERT BEFORE:**
> ### v2.2 changelog (from v2.1)

**INSERTION:**

### v2.3 changelog (from v2.2)

Closes 7 reconciliation gaps between v2.0 (intent growth, retrospective
revision, categorical principles) and v2.1/v2.2 (place generativity,
disclosure preview, register neutrality). No new sections; clarifying
inserts into existing sections only.

- **§17**: Place-carrier evolution registers as a §10 intent layer
  (relation 揭示 or 修正); §11 retrospective revision applies to scenes
  written under the prior carrier assumption (Gap 1)
- **§11**: Retrospective revision inventory covers all four context-gated
  categories ◎ ✦ ✨ ✧, not only ◎; auditing ◎ alone silently misses
  place-carrier drift (Gap 2)
- **§10**: Observed-trigger gains a place-carrier analog — ✧-pattern
  accumulation on one undeclared coordinate, surfaced once with
  three-option interpretation (Gap 3)
- **§18**: Disclosure preview is module-independent; for drives that skip
  situate.map, the preview runs at project creation immediately after
  drive-type selection (Gap 4)
- **§6**: ✧ fires regardless of declared place-shape; baseline is the
  prose when shape is undeclared; undeclared state noted as context,
  not penalized or inferred from (Gap 5)
- **§7**: Center type and drive type are orthogonal; all 16 matrix cells
  (4 drives × 4 center types) are valid; drive type governs
  initialization flow only (Gap 6)
- **§5**: Late-invoked situate.act bundles segment-structure, ✧-implied
  place-shape, and unseen disclosure preview into a single §A.10
  Commitment Confirmation observation, each component preserving its
  three-option interpretation (Gap 7)
